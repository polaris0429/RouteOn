<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <!--
        naviContainer:
        - paddingBottom은 onCreate에서 peekHeight(고정값)로 딱 한 번만 설정됨
        - clipToPadding 기본값(true) → KNNaviView가 padding 영역 밖으로 그려지지 않아
          바텀시트 peekHeight 바로 위까지만 지도 UI가 표시됨
        - onSlide에서 paddingBottom을 변경하지 않으므로 시트를 올려도 지도 크기 고정
    -->
    <FrameLayout
        android:id="@+id/naviContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>

    <!-- 배송 완료 버튼 -->
    <Button
        android:id="@+id/btnCompleteTrip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="100dp"
        android:text="배송 완료"
        android:visibility="gone"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:textColor="#FFFFFF"
        app:backgroundTint="@color/brand_green"/>

    <!-- 바텀 시트 (운행 목록) -->
    <LinearLayout
        android:id="@+id/bottomSheet"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg_bottom_sheet"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
        app:behavior_peekHeight="80dp"
        app:behavior_hideable="false">

        <!-- 드래그 핸들 -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="6dp"
            android:background="@color/drag_handle"/>

        <!-- 타이틀: 가운데 정렬 -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="배차 목록"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="@color/text_primary"
            android:gravity="center"
            android:paddingTop="4dp"
            android:paddingBottom="8dp"/>

        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/run_list_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="100dp"/>

        </ScrollView>

    </LinearLayout>

    <!-- 설정 FAB (바텀시트 완전 펼쳐졌을 때 오른쪽 하단) -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/btnSettings"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="20dp"
        android:src="@android:drawable/ic_menu_preferences"
        android:contentDescription="설정"
        android:visibility="gone"
        app:backgroundTint="@color/brand_green"
        app:tint="#FFFFFF"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
