<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- 네비게이션 뷰 컨테이너 -->
    <FrameLayout
        android:id="@+id/naviContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="92dp" />

    <!-- 배송 완료 버튼 -->
    <Button
        android:id="@+id/btnCompleteTrip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="120dp"
        android:text="배송 완료"
        android:visibility="gone"
        android:paddingStart="24dp"
        android:paddingEnd="24dp"
        android:textColor="#FFFFFF"
        app:backgroundTint="@color/brand_green"/>

    <!-- =====================================================
         휴게소 휴식 오버레이
         - rest_stop 150m 이내 진입 시 표시
         - naviContainer 위에 그려지고 bottomSheet 아래에 위치
         - 배경(반투명 딤): 터치 시 오버레이 일시 숨김
         - 중앙 카드:      항상 불투명
         ===================================================== -->
    <FrameLayout
        android:id="@+id/restStopOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="92dp"
        android:background="#B3000000"
        android:visibility="gone">

        <!-- 반투명 배경 터치 감지 레이어 (카드 밖 영역) -->
        <View
            android:id="@+id/restStopDimLayer"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@android:color/transparent"
            android:clickable="true"
            android:focusable="true" />

        <!-- 중앙 카드 (항상 불투명, dimLayer 위에 배치) -->
        <LinearLayout
            android:id="@+id/restStopCard"
            android:layout_width="300dp"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:background="@drawable/rest_stop_card_bg"
            android:elevation="8dp"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="36dp">

            <!-- 주차 아이콘 -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="6dp"
                android:text="🅿"
                android:textSize="52sp" />

            <!-- 휴게소 이름 -->
            <TextView
                android:id="@+id/tvRestStopName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="14dp"
                android:gravity="center"
                android:text=""
                android:textColor="#AAAAAA"
                android:textSize="14sp" />

            <!-- 타이틀 -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginBottom="28dp"
                android:gravity="center"
                android:text="휴식시간입니다."
                android:textColor="#FFFFFF"
                android:textSize="26sp"
                android:textStyle="bold" />

            <!-- 카운트다운 타이머 -->
            <TextView
                android:id="@+id/tvRestTimer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="monospace"
                android:gravity="center"
                android:includeFontPadding="false"
                android:text="15:00"
                android:textColor="#4CAF50"
                android:textSize="64sp"
                android:textStyle="bold" />

            <!-- 남은 시간 라벨 -->
            <TextView
                android:id="@+id/tvRestTimerLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:gravity="center"
                android:text="남은 휴식 시간"
                android:textColor="#888888"
                android:textSize="13sp" />

            <!-- 개발자 모드 데모 전용: 건너뛰기 버튼 (기본 GONE, 개발자 모드시 VISIBLE) -->
            <Button
                android:id="@+id/btnRestStopSkip"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginTop="20dp"
                android:paddingStart="20dp"
                android:paddingEnd="20dp"
                android:text="건너뛰기 (데모)"
                android:textSize="12sp"
                android:textColor="#999999"
                android:backgroundTint="#2A2A2A"
                android:visibility="gone" />

        </LinearLayout>
    </FrameLayout>

    <!-- 바텀 시트 -->
    <LinearLayout
        android:id="@+id/bottomSheet"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@color/bg_bottom_sheet"
        app:layout_behavior="com.google.android.material.bottomsheet.BottomSheetBehavior"
        app:behavior_peekHeight="80dp"
        app:behavior_hideable="false">

        <!-- 드래그 핸들 -->
        <View
            android:layout_width="40dp"
            android:layout_height="4dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="10dp"
            android:layout_marginBottom="6dp"
            android:background="@color/drag_handle"/>

        <!-- 헤더: 타이틀 + 운행기록 버튼 -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="16dp"
            android:paddingEnd="12dp"
            android:paddingBottom="8dp">

            <!-- 운행 기록 버튼 -->
            <TextView
                android:layout_width="283dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:paddingStart="26dp"
                android:text="@string/main_trip_list_title"
                android:textColor="@color/text_primary"
                android:textSize="16sp"
                android:textStyle="bold" />

            <ImageView
                android:id="@+id/btnTripHistory"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/ic_list"
                android:background="@null"
                android:contentDescription="운행기록"
                android:clickable="true"
                android:focusable="true"
                android:padding="4dp" />

        </LinearLayout>

        <!-- 배차 목록 스크롤 영역 -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <LinearLayout
                android:id="@+id/run_list_container"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:paddingStart="16dp"
                android:paddingEnd="16dp"
                android:paddingBottom="96dp"/>

        </ScrollView>

    </LinearLayout>

    <!-- 하단 액션 바 -->
    <LinearLayout
        android:id="@+id/bottomActionBar"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:orientation="horizontal"
        android:layout_gravity="bottom"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="20dp"
        android:gravity="center_vertical"
        android:visibility="gone">

        <!-- 설정 FAB -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnSettings"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_settings"
            android:contentDescription="설정"
            app:backgroundTint="@color/brand_green"
            app:tint="#FFFFFF"
            app:fabSize="normal"/>

        <!-- 도움 버튼 -->
        <TextView
            android:id="@+id/btnHelp"
            android:layout_width="0dp"
            android:layout_height="44dp"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:text="@string/main_help_text"
            android:textSize="13sp"
            android:textColor="@color/brand_green"
            android:textStyle="bold"
            android:gravity="center"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:background="@drawable/bg_help_button"
            android:clickable="true"
            android:focusable="true"
            android:elevation="2dp"/>

        <!-- 채팅 FAB -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/btnChat"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_chat"
            android:contentDescription="채팅 문의"
            app:backgroundTint="@color/brand_green"
            app:tint="#FFFFFF"
            app:fabSize="normal"/>

    </LinearLayout>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
